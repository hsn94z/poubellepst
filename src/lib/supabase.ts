/**
 * Supabase JS (project dependency) — wire `createClient` when Bolt / backend URLs are ready.
 * @see https://supabase.com/docs/reference/javascript/introduction
 */
export type { SupabaseClient } from '@supabase/supabase-js'
